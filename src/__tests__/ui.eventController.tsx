describe('-', () => {
  describe('일정 추가 시, 일정 관리 화면에 추가한 일정이 표시된다.', () => {
    it('추가한 일정이 반복 주기 설정에 따라 캘린더 뷰에 표시된다.', async () => {
      expect(true).toBe(false);
    });

    it('캘린더 뷰에 반복일정이 시각적으로 표시되어 구분된다.', async () => {
      expect(true).toBe(false);
    });

    it('캘린더 뷰에 반복 종료일이 있다면 표시된다.', async () => {
      expect(true).toBe(false);
    });
  });

  describe('일정 수정.', () => {
    it('반복 설정된 일정을 수정할 때, "모든 일정 수정"버튼을 누르면 일괄 수정할 수 있다.', async () => {
      expect(true).toBe(false);
    });

    it('"해당 일정만 삭제"버튼으로 해당 일정만 삭제할 수 있다.', async () => {
      expect(true).toBe(false);
    });

    it('"전체 일정 삭제"버튼으로 반복 일정을 삭제할 수 있다.', async () => {
      expect(true).toBe(false);
    });
  });
});
