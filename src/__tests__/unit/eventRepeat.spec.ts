describe('getRepeatEvents', () => {
  it('O일 마다 반복된 이벤트들이 반환된다.', () => {});

  it('O 주 마다 반복된 이벤트들이 반환된다.', () => {});

  it('O 달 마다 반복된 이벤트들이 반환된다.', () => {});

  it('O년 마다 반복된 이벤트들이 반환된다.', () => {});

  it('잘못된 반복 유형이 있으면 에러가 난다.');
});
