import { Event, RepeatEvent } from '../types';

export const getRepeatingEvent = (events: Event[]): RepeatEvent => {
  return {};
};
export const getMaxRepeatLength = (event: Event) => {
  return 9999;
};
